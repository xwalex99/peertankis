service: peertankis

# Usa el Dockerfile (App Engine Flexible)
runtime: custom
env: flex

env_variables:
  NODE_ENV: "production"
  HOST: "0.0.0.0"
  PORT: "8080"
  WS_PATH: "/ws"
<<<<<<< HEAD
  MAX_ROOM_PLAYERS: "10"
  ROOM_IDLE_TTL_MS: "60000"
  MESSAGE_MAX_BYTES: "65536"
  RATE_LIMIT_PER_SEC: "50"

# Room state is in memory; start with a single instance unless you add shared storage.
=======

# WebSocket server es stateful; por seguridad, empieza con 1 instancia.
>>>>>>> aa737854ad604761e08b2bd2bebb052c8d28a753
automatic_scaling:
  min_num_instances: 1
  max_num_instances: 1
