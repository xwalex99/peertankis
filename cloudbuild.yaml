substitutions:
  _REGION: europe-southwest1
  _AR_REPO: peertankis
  _IMAGE: peertankis
  _SERVICE: peertankis

options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/${_IMAGE}:${COMMIT_SHA}', '.']
    timeout: '1200s'  # Aumentar tiempo de construcción para instalar dependencias

  # Subir la imagen a Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/${_IMAGE}:${COMMIT_SHA}']
    timeout: '600s'  # Aumentar tiempo de subida

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "run"
      - "deploy"
      - "${_SERVICE}"
      - "--image"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/${_IMAGE}:${COMMIT_SHA}"
      - "--region"
      - "${_REGION}"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--port"
      - "8080"
      - "--set-env-vars"
<<<<<<< HEAD
      - "HOST=0.0.0.0,WS_PATH=/ws,MAX_ROOM_PLAYERS=10,ROOM_IDLE_TTL_MS=60000,MESSAGE_MAX_BYTES=65536,RATE_LIMIT_PER_SEC=50"
=======
      - "HOST=0.0.0.0,WS_PATH=/ws,NODE_ENV=production"
>>>>>>> aa737854ad604761e08b2bd2bebb052c8d28a753

images:
  - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_AR_REPO}/${_IMAGE}:${COMMIT_SHA}"

timeout: "2400s"
