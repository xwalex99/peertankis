# PeerJS Server config
HOST=0.0.0.0
PORT=9000
PEER_PATH=/peerjs
PEER_PROXIED=true
PEER_ALLOW_DISCOVERY=false
# Recommended: use a non-public key (client must match)
PEER_KEY=tankis-peer

# TURN Server config (Coturn)
# Habilitar/deshabilitar TURN (true/false)
ENABLE_TURN=true
# Realm del servidor TURN (debe ser el dominio público del servidor)
TURN_REALM=peertankis-1093381928939.europe-southwest1.run.app
# Usuario para autenticación TURN
TURN_USER=tankis-turn
# Contraseña para autenticación TURN
TURN_PASSWORD=tankis-turn-secret


